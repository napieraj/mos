# CONTRIBUTING.md

## Code layout

Idiomatic C: public headers in `include/`, implementation in `src/`,
the CLI that links the library in `cli/`, unit tests in `tests/`.

```
mos/
├── README.md
├── ARCHITECTURE.md              # single source of truth for design
├── ROADMAP.md                   # version arcs and deferred work
├── INTEGRATION_HARNESS.md       # hardware matrix + validation gate
├── CONTRIBUTING.md              # this file
├── AGENTS.md                    # LLM-agnostic project context + ADRs
├── CLAUDE.md                    # Claude-specific failure modes
├── LICENSE                      # 0BSD
├── CMakeLists.txt               # primary build system
├── Makefile                     # thin wrapper over cmake
├── .editorconfig
├── .gitignore
├── .github/workflows/ci.yml     # build/test/sanitize/fuzz/schema jobs
├── dist/                        # committed amalgamation artifact
│   ├── mos.h                    #   (regenerated by scripts/amalgamate.sh;
│   ├── mos.c                    #    CI diffs committed vs regenerated)
│   └── MANIFEST.txt
├── doc/
│   ├── research/                # frozen dated research artifacts
│   └── dr-field-mapping.md      # DR-substrate field source mapping
├── docs/                        # NOT tracked documentation: local-only
│                                #   vendored SDK headers (docs/apple/),
│                                #   stripped from archives by preflight
├── include/
│   └── mos.h                    # ONLY public header — API surface
├── src/
│   ├── mos_internal.h           # private; IOKit-coupled struct definitions
│   ├── mos_pure.h               # private; pure-data prototypes (no IOKit)
│   ├── mos_scsi_status.h        # SAM-5 status constants
│   ├── mos_pure.c               # pure helpers (BSD-name, status, IOReturn map)
│   ├── mos_sense.c              # pure sense parser + state mapping
│   ├── mos_config.c             # pure GET CONFIGURATION feature walk
│   ├── mos_discinfo.c           # pure READ DISC INFORMATION decode
│   ├── mos_result.c             # pure opaque-struct accessors
│   ├── mos_strings.c            # pure error-string and version tables
│   ├── mos_state_core.c         # pure decision tree (testable without IOKit)
│   ├── mos_watch_core.c         # pure watch state machine (testable without IOKit)
│   ├── mos_state.c              # adapter: fills env from handle, calls core
│   ├── mos_watch.c              # IOKit watch adapter (poll pump + DR doorbell)
│   ├── mos_scsi.c               # IOKit lifecycle, MMC convenience primitives, raw CDB
│   └── mos_query.c              # typed mos_query_* verbs (decode MMC replies)
├── cli/
│   ├── main.c                   # argument parsing + dispatch
│   ├── common.c / .h            # shared CLI state, list rows, envelopes
│   ├── status.c / list.c / watch.c  # one file per command
│   ├── probe.c                  # diagnostic substrate observer ('mos probe', MOS_CLI_PROBE)
│   ├── io.c / .h                # output/escaping helpers
│   └── human.c / .h             # human-readable layout engine
├── tests/
│   ├── test_harness.h           # tiny TEST/RUN/EXPECT macros
│   ├── test_main.c              # aggregator
│   ├── test_sense.c             # sense parsing and state-map tests
│   ├── test_bsd_name.c          # BSD name normalization tests
│   ├── test_scsi_status.c       # SAM-5 contention classifier tests
│   ├── test_ioreturn.c          # IOReturn → mos_error mapping tests
│   ├── test_state_core.c        # decision tree integration tests (fake MMC)
│   ├── test_watch_core.c        # watch state machine tests (fake ops)
│   ├── test_render.c            # JSON / safe-ASCII hostile-input tests
│   ├── test_config.c            # GET CONFIGURATION walk hostile-input tests
│   ├── test_discinfo.c          # READ DISC INFO decode tests
│   ├── test_result.c            # opaque accessor tests
│   ├── test_watch_lifetime.c    # macOS ASan string-lifetime guard (CI-linked)
│   ├── fuzz_pure.c              # pure-layer fuzz harness (ASan/UBSan CI job)
│   ├── fixtures/                # spec-derived + contributed captures
│   └── cli/test_cli.sh          # CLI contract tests (run against built binary)
├── schemas/                    # formal JSON Schemas + positive/negative fixtures
│   ├── README.md
│   ├── validate.py             # CI script: examples + negatives + enum drift
│   ├── requirements-ci.txt     # hash-pinned validator deps
│   ├── mos.state.v1.json
│   ├── mos.error.v1.json
│   ├── mos.list.v1.json
│   ├── mos.event.v1.json
│   ├── examples/               # payloads that MUST validate
│   └── negative/               # payloads that MUST be rejected
├── homebrew/
│   └── mos.rb                  # tap formula (HEAD-only between tags)
└── scripts/
    ├── amalgamate.sh            # stb-style single-file drop-in build
    └── release-preflight.sh     # archive hygiene gate
```

## Symbol-naming conventions

This library is designed to be statically linked into larger binaries
(HandBrake, VLC, ARM). Symbol collisions on generic names like
`open_device` or `parse_sense` are unacceptable.

Rules:

1. **Public API:** prefixed `mos_` and declared in `include/mos.h`.
   Nothing else crosses the library boundary.
2. **Internal but shared between `.c` files:** prefixed
   `mos_internal_` and declared in `src/mos_internal.h` (never in
   `include/`).
3. **File-local:** `static`. No exceptions.

CI enforces this via `nm` on both built static libraries
(`libmos.a` and `libmos_pure.a` — see CMakeLists.txt for the
two-tier split rationale). Non-`mos_` symbols fail the build.

The `nm` check covers the library archives only; the `mos` CLI is a
separate binary. Within `cli/`, every cross-translation-unit identifier
(header-declared functions and types) carries a `mos_cli_` prefix —
`mos_cli_run_query`, `mos_cli_emit_list_table`, `mos_cli_list_row`,
`mos_cli_human_pair`, the `mos_cli_stdout_*` family. File-local `static`
symbols and the parse-state `extern` globals (`opt_*`, `flag_*`,
`progname`) are exempt: the prefix exists to keep cross-TU names from
colliding, which `static` cannot, and the argv-parse globals are a
deliberate file-spanning state block named for the options they hold.

### `_t` suffix

The suffix is not decorative; it marks a category, so a reader can tell
a handle from a value at the use site:

- **`_t`** — opaque types the caller holds across calls (`mos_handle_t`,
  `mos_watch_t`), opaque iteration subjects handed to a callback
  (`mos_device_info_t`, `mos_feature_info_t`), and internal ops-vtable /
  environment structs (`mos_mmc_ops_t`, `mos_state_env_t`,
  `mos_watch_ops_t`).
- **no `_t`** — query-result objects read through accessors
  (`mos_state_result`, `mos_disc_info`, `mos_toc`, `mos_drive_caps`, …),
  public enums (`mos_error`, `mos_state`, `mos_disc_status`,
  `mos_event_kind`, `mos_xfer_dir`), and plain pure-data structs
  (`mos_toc_entry`, `mos_config_feature`, `mos_cli_human_pair`).

A struct exposed publicly as an iteration subject is defined as a bare
tagged `struct` internally and typedef'd to its `_t` name *only* in
`include/mos.h` — `struct mos_device_info` / `struct mos_feature_info`
are the two of these; do not add a second internal typedef alias.

## Comment doctrine

Two rules keep commentary useful and bounded (the survey behind them
is `doc/research/2026-06-11-comment-refactor-plan.md`):

1. **Budget scales with audience.**
   - `include/mos.h` is the documentation of record for external
     consumers, who see only opaque types and accessors. Full caller
     contracts, ownership/lifetime rules, threading, and ABI rationale
     belong there.
   - Internal seam headers (`src/mos_pure.h`, `src/mos_internal.h`,
     `cli/*.h`) address contributors who can open the `.c`. Prototypes
     get one-to-three-line contracts: args, return domain, ownership
     when non-obvious. Exception: a cross-cutting safety contract that
     governs multiple implementations (the dual-length rule) keeps its
     one full statement in the header, because no single `.c` owns it.
   - `.c` files carry why-comments at the code — constraints the code
     cannot express (kernel behavior, spec citations, safety gates) —
     never restated contracts, never narration of the next line.

2. **Each fact has exactly one home.** Caller contract → header.
   Implementation why → `.c`, at the relevant lines. Decisions, dates,
   review/audit provenance → `ARCHITECTURE.md` / `doc/history/` /
   `doc/research/`, with a `§` pointer from code. Spec byte layouts →
   the `.c` that does the parsing. File headers: at most three lines
   (what the file is, plus the one constraint that governs it).
   Cross-reference, never restate.

## What goes in which file

- **`src/mos_sense.c`** — sense parsing, sense-to-state mapping. Pure
  data, no IOKit, no `malloc`, no globals. Trivially unit-testable
  with fixture bytes. Add new sense decoding here; add tests in
  `tests/test_sense.c`.

- **`src/mos_pure.c`** — pure helper functions that conceptually
  belong with the IOKit code but have no IOKit dependency: BSD-name
  normalization, whole-disk shape predicate, SAM-5 contention-status
  classifier, IOReturn-to-mos_error mapping. Lives in its own
  translation unit so the pure-only test target compiles and links
  without any Apple framework. Same constraints as `mos_sense.c` —
  no IOKit, no globals, trivially unit-testable.

- **`src/mos_strings.c`** — pure error-message and version string
  tables. Same constraints.

- **`src/mos_config.c`** — pure, bounds-safe GET CONFIGURATION feature
  walk and current-profile extraction. Device-reported lengths may only
  shrink the trusted region. Tests in `tests/test_config.c`; fuzzed by
  the `fuzz_config` phase.

- **`src/mos_discinfo.c`** — pure READ DISC INFORMATION decode (status
  + session/track counts). Same hostile-length contract. Tests in
  `tests/test_discinfo.c`; fuzzed by the `fuzz_discinfo` phase.

- **`src/mos_result.c`** — pure, NULL-tolerant accessors for the opaque
  `mos_state_result` / `mos_watch_event` types. Public API, woven into
  the amalgamation. Tests in `tests/test_result.c`.

- **`src/mos_state_core.c`** — the pure decision tree for
  `mos_query_state()`. Operates against a `mos_mmc_ops_t` vtable
  whose three callbacks (`get_tray_state`, `test_unit_ready`,
  `get_current_profile`) are the only MMC surface it consumes.
  No IOKit, no CoreFoundation. Tests in `tests/test_state_core.c`
  drive this function with a fake vtable whose callbacks return
  scripted responses, exercising every branch without hardware.

- **`src/mos_state.c`** — thin Apple-side adapter. Fills
  `mos_state_env_t` from a real `mos_handle_t` and forwards to
  `mos_internal_query_state_core()` in `mos_state_core.c`. IOKit-linked
  because it dereferences the handle's IOKit-typed fields.
  Never calls `ObtainExclusiveAccess` directly.

- **`src/mos_scsi.c`** — IOKit lifecycle (`mos_open_*`, `mos_close`),
  enumeration, the MMC convenience primitives the state core uses
  (TUR / current-profile / tray-state), and the raw CDB path
  (`mos_raw_cdb` — the sole `ObtainExclusiveAccess` site). Links against
  `IOKit.framework` and `CoreFoundation.framework`. Pure helpers were
  extracted to `mos_pure.c` during the v0.2 library split.

- **`src/mos_query.c`** — the typed `mos_query_*` verb surface (disc
  info, TOC, CD-TEXT, drive caps, features, disc id, physical structure,
  track info, capacity, perf, mode pages): each verb issues one MMC
  convenience command and hands the reply to a pure decoder. Split out
  of `mos_scsi.c`; every verb is non-exclusive, so it takes no lock.

- **`cli/`** — the CLI front-end, one file per command over a shared
  layer (`main.c` parses with `getopt_long` and dispatches;
  `status.c`/`list.c`/`watch.c`/`probe.c` implement the verbs; JSON
  emission is hand-rolled; the output contract is consumed by
  downstream automation). Links `mos_core`; does not include private
  headers (exception: `cli/probe.c`, a diagnostic compiled in under
  `MOS_CLI_PROBE`, includes `src/mos_pure.h` for the BSD-unit parse).

  On a new machine, `mos list` then `mos status --json` exercise the
  full library path; `mos probe --dump` captures the raw DiscRecording
  dictionaries when enumeration disagrees with expectation. (The
  standalone `tools/` probes were consolidated into `mos probe` on
  2026-06-11 — see the ROADMAP append.)

## What NOT to do

1. **Do not add `ObtainExclusiveAccess` calls to the default query
   path's mounted-disc reach.** A query against a READY drive must
   never take the lock — TUR GOOD short-circuits before any exclusive
   access, so a mounted, in-progress rip is never disturbed. The one
   sanctioned exception is the not-ready branch's GESN tray probe,
   which routes through `mos_raw_cdb()` and is safe precisely because
   not-ready ⇒ not mounted ⇒ the lock is free. `mos_raw_cdb()` remains
   the only call site of `ObtainExclusiveAccess`. See
   `ARCHITECTURE.md` §3.
2. **Do not add transcoding, decoding, or any higher-layer media
   operations.** This library reports drive state. Period.
3. **Do not add fixed-size arrays to the public `mos_state_result`
   struct.** Downstream bridging pain follows. Use borrowed string
   pointers into the handle's own buffers.
4. **Do not include Apple-specific headers in `include/mos.h`.** The
   public header must be compilable under `#include` from a
   cross-platform project. IOKit types live in `src/mos_internal.h`
   only.
5. **Do not add dependencies.** No libplist, no libusb, no Boost.
   CoreFoundation + IOKit + DiscRecording only — those are Apple
   system frameworks, available unconditionally on any supported
   macOS deployment target.
6. **Do not introduce Swift or Objective-C.** This library is pure C
   on purpose — that is the thing that makes it embeddable in
   HandBrake, VLC, Kodi, and friends.
7. **Do not free a `mos_handle_t` before you've consumed its
   `mos_state_result`.** The strings the `mos_state_result_vendor` /
   `_product` / `_revision` accessors return are borrowed pointers
   into the handle's internal buffers — they go stale the moment
   `mos_close()` is called or
   `mos_query_state()` is invoked again. Emit / copy first, close
   after. This was a P1 bug in early v0.2 code; the `cli/` code shows
   the correct ordering.

## Testing

```sh
make test         # pure-data unit tests (no hardware)
mos probe         # hardware smoke test subcommand (requires an optical drive)
```

Unit tests use a tiny `TEST / RUN / EXPECT / EXPECT_EQ / EXPECT_STREQ`
macro set in `tests/test_harness.h`. No framework dependency.

To add a new test: append a `TEST(name) { ... }` function to the
appropriate `test_*.c`, register it in that file's
`register_*_tests()`, and re-run `make test`.

## Amalgamated single-file distribution

For projects that want a literal drag-and-drop header + source pair
(stb / SQLite style), run:

```sh
./scripts/amalgamate.sh
```

This concatenates `include/mos.h` into `dist/mos.h` and weaves
`src/mos_scsi_status.h` + `src/mos_pure.h` + `src/mos_internal.h` +
every `src/*.c` translation unit into `dist/mos.c`, pure state
machines grouped before the IOKit adapters for readability. The
script's own file list (in `scripts/amalgamate.sh`) is the
authoritative order — it is not duplicated here, because a duplicated
list rots. CI diffs the committed `dist/` against a fresh
regeneration on every push, so script and artifact cannot drift
silently.
Drop those two files into your tree and you're done — no submodule,
no CMake subdirectory.

When compiling the amalgamated output yourself, mirror what the
CMake `mos_core` target does: link three Apple frameworks and set
the macOS deployment-target floor that matches the project
(`MOS_VERSION_*` is independent of the OS floor; CMakeLists.txt
pins `CMAKE_OSX_DEPLOYMENT_TARGET 12.0`). Example:

```sh
cc -c dist/mos.c -mmacosx-version-min=12.0 -o mos.o
cc your_main.o mos.o -o your_tool \
   -framework IOKit \
   -framework CoreFoundation \
   -framework DiscRecording \
   -mmacosx-version-min=12.0
```

Skipping `-framework DiscRecording` will fail to link at the
`DRCopyDeviceArray` reference in `mos_dr.c`; skipping the
deployment-target flag will let the build silently float to
whatever default the host SDK provides, which may not match what
the CMake build is tested against.

## Upstreaming into other projects

The explicit non-goal of this repo is to be absorbed by larger
projects. If you want to pull parts of this into ARM, HandBrake, Kodi,
VLC, or similar:

- **Just `src/mos_sense.c`?** It has no dependencies (the sense parser
  reads 18 bytes and writes three values). Take it.
- **Full library?** Use `CMakeLists.txt` via `add_subdirectory` or
  `FetchContent`, or the amalgamated single-file output.
- **Attribution?** 0BSD, so not required. Appreciated though.

## Release workflow

The Homebrew formula at `homebrew/mos.rb` is intentionally HEAD-only
between tagged releases — no `url` / `sha256` fields are present.
This avoids the placeholder-SHA256 footgun, where a committed-but-not-
yet-filled-in sha256 would cause every `brew install mos` during the
window to fail with a checksum error that looks like a broken
formula.

**Tagging v0.X.Y:**

1. Confirm CI green on `main` — all jobs, including the full pure
   suite (live count is in the CI log, never hardcoded here) and the
   amalgamation job.
2. `git tag -a vX.Y.Z -m "..."` and `git push --tags`.
3. GitHub auto-generates a source tarball for the tag.
4. Fetch and hash it:
   ```sh
   curl -sL https://github.com/napieraj/mos/archive/refs/tags/vX.Y.Z.tar.gz \
     | shasum -a 256
   ```
5. In a single commit to `homebrew/mos.rb`, add:
   ```ruby
   url "https://github.com/napieraj/mos/archive/refs/tags/vX.Y.Z.tar.gz"
   sha256 "<the hash>"
   ```
   Or equivalently, run `brew bump-formula-pr --url=... --sha256=...`
   if the tap is structured for it.
6. Push. `brew install mos` now works without `--HEAD`.

**Non-goals of this workflow:**

Do not pre-commit a placeholder sha256 and an `odie` guard to detect
it. That pattern causes real installs to fail during the gap between
tag-push and sha-fill with a message users can't act on. Keep the
formula head-only until you have the real hash.

## PR checklist

- [ ] New public API additions have `mos_` prefix and live in
      `include/mos.h`.
- [ ] New internal helpers have `mos_internal_` prefix or are `static`.
- [ ] No new fixed-size arrays in public structs.
- [ ] `make test` passes.
- [ ] If you added a code path that issues a raw CDB, it's behind
      `mos_raw_cdb()` and not on the default query path.
- [ ] `ARCHITECTURE.md` updated if decision-tree or state semantics
      changed.
- [ ] Hardware fixture added to `tests/fixtures/` if your change was
      validated against real hardware.
